<%= form_for(@weather_item) do |f| %>
  <% if @weather_item.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@weather_item.errors.count, "error") %> prohibited this weather_item from being saved:</h2>

      <ul>
      <% @weather_item.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
